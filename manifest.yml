---
applications:
 - name: bettersleep
   routes:
   - route: bettersleep.eu-gb.mybluemix.net
   memory: 128M
   command: gunicorn wsgi:app --preload & python -m main.keep_checking
   services:
   - sqldatabase
