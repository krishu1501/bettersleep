---
applications:
 - name: bettersleep
   routes:
   - route: bettersleep.eu-gb.mybluemix.net
   memory: 128M
   command: gunicorn wsgi:app --preload
   services:
   - sqldatabase
applications:
 - name: bettersleep
   memory: 128M
   command: python -m main.keep_checking
   services:
   - sqldatabase
